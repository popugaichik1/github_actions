name: Fourteen workflow
on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
        #клонирует репо
      - uses: actions/checkout@v4
      
        #кэширует npm-пакеты
      - name: Cache node_modules
        id: cache-step
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-cache-file-${{ hashFiles('**/package-lock.json')}}
          restore-keys: |
            ${{ runner.os }}-cache-file-
            ${{ runner.os }}-
      
        #выводит инфу о кэше(если кэш есть то он не создается повторно)
      - name: Display Cache
        run: echo '${{ toJSON(steps.cache-step.outputs) }}'
      
        #инсталирует заивисимости
      - name: Install node_modules
        run: npm install