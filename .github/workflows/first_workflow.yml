name: First_workflow
on: workflow_dispatch

jobs: 
    run-first-command: 
        runs-on: ubuntu-latest
        steps:
            - name: Display some Text
              run: echo "some text"
            - name: Command in multiline
              run: |
                node -v
                npm -v
