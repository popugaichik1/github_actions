name: First_workflow
on: push

jobs: 
    run-first-command: 
        runs-on: ubuntu-latest
        steps:
            - name: Display some Text
              run: echo "some text"
            - name: Command in multiline
              run: |
                node -v
                npm -v

                