name: Third workflow
on: push

jobs: 
  run-first-command: 
    runs-on: ubuntu-latest
    steps:
      - name: List files 
        run: ls -aцвцвы

      - name: Copy files 
        run: |
          git init
          git remote add origin "https://$GITHUB_ACTOR:${{secrets.GITHUB_TOKEN}}@github.com/popugaichik1/github_actions.git"
          git fetch origin
          git checkout main

      - name: List Files
        run: ls -a